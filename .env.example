# =============================================================================
# CLIENT (Expo) — apenas valores públicos por design (RLS protege os dados)
# NUNCA coloque GEMINI_API_KEY, OPENAI_API_KEY, WEATHER_API_KEY ou SERVICE_ROLE aqui.
# =============================================================================
EXPO_PUBLIC_API_URL=https://show-me-the-wine-api.showmethewine.workers.dev

# =============================================================================
# SERVER (Cloudflare Worker secrets) — não colocar no app:
#   npx wrangler secret put DATABASE_URL
#   npx wrangler secret put AUTH_SECRET
#   npx wrangler secret put GEMINI_API_KEY
#   npx wrangler secret put WEATHER_API_KEY
# =============================================================================
